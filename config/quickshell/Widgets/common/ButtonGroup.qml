import QtQuick
import QtQuick.Layouts

// Horizontal button group — used in notification status bar row.
RowLayout {
    id: root
    spacing: 6

    // Children are typically GroupButton instances.
    // The RowLayout handles sizing and alignment automatically.
}
